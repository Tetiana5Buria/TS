{
  "name": "ts",
  "version": "1.0.0",
  "description": "A simple TypeScript project",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && tsc",
   "start": "node dist/myFunction.js & node dist/index.js & node dist/bagFunction.js"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
